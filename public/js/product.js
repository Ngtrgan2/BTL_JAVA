// Placeholder cho code quản lý sản phẩm Frontend
document.addEventListener('DOMContentLoaded', () => {
    console.log('Product JS Loaded');
    // Sẽ gọi API.getProducts() ở đây để load danh sách
});
